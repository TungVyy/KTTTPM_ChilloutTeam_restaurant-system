server:
  port: 8083

spring:
  application:
    name: staff-service
  datasource:
    url: jdbc:mysql://localhost:3307/librarydb?useSSL=false&serverTimezone=UTC
    username: root
    password: 123456
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true